{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Twitter Followers Inputs Schema",
  "description": "Schema for configuring one or more Twitter follower sources to scrape.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["source_url"],
    "properties": {
      "source_url": {
        "type": "string",
        "format": "uri",
        "description": "Twitter API endpoint or public profile URL to scrape followers from."
      },
      "type": {
        "type": "string",
        "enum": ["api", "html"],
        "default": "api",
        "description": "Hint about whether the source is a JSON API endpoint or an HTML page."
      },
      "metadata": {
        "type": "object",
        "description": "Optional metadata for internal tracking or labeling.",
        "additionalProperties": true
      }
    },
    "additionalProperties": false
  }
}